dependencies {
    api 'com.cloudinary:cloudinary-http44:1.29.0'

    api project(':pm-domain')
    api project(':pm-user-service')

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}
